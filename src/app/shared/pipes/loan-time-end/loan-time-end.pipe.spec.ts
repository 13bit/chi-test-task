import { LoanTimeEndPipe } from './loan-time-end.pipe';

describe('LoanTimeEndPipe', () => {
  it('create an instance', () => {
    const pipe = new LoanTimeEndPipe();
    expect(pipe).toBeTruthy();
  });
});
